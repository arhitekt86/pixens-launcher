# Build output
dist/
out/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/

# Generated binaries (downloaded / produced at build time)
build/ffmpeg.exe

# Realms game payload (downloaded on demand; lives in its own repo)
staging/realms-*.zip

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# Env (none currently — guard for the future)
.env
.env.*
!.env.example

# Local SignPath / cert artefacts (never commit)
*.pfx
*.p12
*.key
*.pem
signpath/

# Logs
*.log
